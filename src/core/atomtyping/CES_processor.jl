using BiochemicalAlgorithms


function CES_parser()
    # check grammar of current top position in DEF
    # build string with same grammar for current atom atom/molecule information
    # return bool, move to next position in DEF file or end search on positive 
end

function CES_processor()
    # use DEF file and information from CES_parser process to cycle through
    # DEF file until Atomtype is assigned or no more criteria fit (return error?)
end